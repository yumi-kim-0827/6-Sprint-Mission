import React from "react";
import styled from "styled-components";

const NotFoundPage = () => {
  return <StyledDiv>NotFoundPage</StyledDiv>;
};

const StyledDiv = styled.div`
  margin: 70px auto;
  padding: 48px;
  max-width: 1200px;
  height: calc(100vh - 230px);
  font-weight: 700;
  font-size: 36px;
`;

export default NotFoundPage;
