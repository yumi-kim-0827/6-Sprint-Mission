import styles from "./styles.module.css";

function AuthenticatedProfileButton() {
  return (
    <button
      className={styles.authenticatedProfileButton}
      type="button"
    ></button>
  );
}

export default AuthenticatedProfileButton;
