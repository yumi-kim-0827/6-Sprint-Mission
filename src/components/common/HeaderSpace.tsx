import styles from '@/styles/common/headerSpace.module.css';

/**
 * header 공간 컴포넌트
 * @function HeaderSpace
 * @returns {JSX.Element} view
 */
const HeaderSpace: React.FC = () => {
	return <div className={styles.headerSpace}></div>;
};

export default HeaderSpace;
