import { createDescriptionContent } from "../common/common";

const emailVaildEventHandler= ()=>{
  
}

// .description-invaild 이거 밑에 넣어야 되고, 
// content-form__input--invalid , valid 써야된다.
const passwordVaildEventHandler = () => {
  
};